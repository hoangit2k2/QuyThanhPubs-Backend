version: '3.8'

services:
    # nestapp:
    #     container_name: nestapp
    #     image: francescoxx/nestapp:1.0.0
    #     build: .
    #     ports:
    #         - '3000:3000'
    #     environment:
    #         - DB_TYPE=postgres
    #         - PG_USER=postgres
    #         - PG_PASSWORD=postgres
    #         - PG_DB=postgres
    #         - PG_PORT=5432
    #         - PG_HOST=db
    #     depends_on:
    #         - dev-db

    dev-db:
        image: postgres:13
        ports:
            - 5432:5432
        environment:
            POSTGRES_USER: admin
            POSTGRES_PASSWORD: admin
            POSTGRES_DB: nest
        networks:
            - database

networks:
    database: