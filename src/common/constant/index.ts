export enum ROLE {
  ADMIN = 'admin',
  STAFF = 'staff',
}

export enum ORDER_PRODUCT_STATUS {
  DELIVERED = 'delivered',
  NOT_YET_DELIVERED = 'not_yet_delivered',
}

export enum TABLE_STATUS {
  UNPAID = 'unpaid',
  PAID = 'paid',
  SERVING = 'serving',
}
